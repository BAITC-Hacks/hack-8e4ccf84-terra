# Касымжан / S05 — передача UI владельцу интеграции

Четыре маршрута: `/overview`, `/forecast`, `/sources`, `/agent-log`. UI реализован в `src/app/`; тонкие re-export маршруты в корневом `app/` сохраняют совместимость с параллельно интегрированным API-адаптером S06. Его файлы не менялись. `src/server/**`, API, миграции и root-конфиги не менялись.

UI-local схемы: `src/components/dashboard/contracts.ts`; единственная граница транспорта: `client.ts`. Подробные предложения wire-контракта и команды проверки: [tests/ui/README.md](../../tests/ui/README.md).

Запрос S01/S02/S04: согласовать envelope ответов, канонические имена полей/единиц, multipart mapping/options, получение списка прогнозов с точками и пагинацию. После публикации контрактов изменить только UI-адаптер и выполнить реальный E4 сценарий. S07/S08: согласовать discovery запусков, result_id задач, отчёты оценки и экспорт; пока поддержаны пути из PLAN §8.

Fixture и метрики явно синтетические. Ошибки API не заменяются fixture. С00: исходная нормализованная шкала, без перевода в МВт/процент номинала; зона и смысл времени требуют явного выбора. UI не объединяет исходные ряды в мощность станции.

Проверено: production build + TypeScript, ESLint, 6 модульных/API-adapter тестов, 13 браузерных сценариев Edge (fixture и mock API), desktop 1440 и mobile 390 px. Реальный API отсутствует на проверенной базе; это открытый gate, не успешная интеграция E4.
